steps:
  # Install dependencies
  - name: python
    entrypoint: pip
    args: ["install", "-r", "server/requirements.txt", "--user"]

  # Docker Build
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t',
           'us-central1-docker.pkg.dev/$PROJECT_ID/$_REPO_NAME/fmltc:$SHORT_SHA', '.']
